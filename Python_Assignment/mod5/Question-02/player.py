squad={}

n=int(input('Enter the no. of players'))

for i in range(n):
    squad[input('Type of the player')]={input('name'):{'matches':input('matches'),'Runs':input('Runs'),'Average':input('Average'),'Highest':input('Highest Score')}}
print(squad)

    
    
